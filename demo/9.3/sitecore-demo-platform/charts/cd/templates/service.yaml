apiVersion: v1
kind: Service
metadata:
  name: {{ include "cd.fullname" . }}
  labels:
    {{- include "cd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "cd.selectorLabels" . | nindent 4 }}
